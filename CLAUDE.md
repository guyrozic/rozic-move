# rozicmove.com — הקשר פרויקט

הריפו הזה היה **חסר תיעוד לחלוטין** עד 15.9.2026 — אין בו היסטוריה של
`README`, `CLAUDE.md` או `.claude/`. הקובץ הזה נוצר כדי שמי שפותח אותו
יידע את הדברים שאי אפשר לגזור מהקוד.

⚠️ **הקובץ הזה לא מחזיק "מצב נוכחי" בכוונה.** מצב הגרסאות, מה נפרס ומה
ממתין — הכול ב-`~/Hovalot/CHANGELOG.md` וב-`~/Hovalot/CLAUDE.md`, ריפו
אחד, מקור אמת אחד. סטטוס שנכתב כאן היה מתיישן ומשקר, וזה בדיוק מה שקרה
לתיעוד הקודם של האתר.

## מי אני
**עמוס** — הסמנכ"ל של חברת ההובלות. האתר והאפליקציה הם אותו מוצר ואותו
פרויקט Firebase, ולכן אותו סוכן. (עמית = כל השאר, אסף = עוזר הבית.)

## פריסה — כל קומיט הוא ייצור
GitHub Pages מ-`guyrozic/rozic-move` ענף `main`, `CNAME` → `rozicmove.com`.
**אין workflow, אין שלב build, ואין סביבת בדיקות.** מה שנדחף ל-`main`
עולה לאוויר. אין "לדחוף ואז לבדוק".

## סטאק
JS וניל — לא TypeScript, לא bundler, לא npm install. Firebase JS SDK
v12.16.0 נטען מ-CDN. `app/` הוא זרימת ההזמנה המחוברת (מאחורי
`requireAuth()`); שאר הדפים בשורש הם התוכן הפומבי.

## ⚠️ הכלל החשוב ביותר: האתר מחזיק עותק שני של לוגיקה עסקית
תמחור, קטלוג הפריטים, נוסח התקנון ומדרג הביטולים קיימים **גם כאן וגם
באפליקציה**. ב-15.9 נמצאו שישה מקומות שבהם שני העותקים לא הסכימו, וכולם
נגעו בכסף — ביטוח שנגבה בניגוד לתקנון, מנוף שהיה חינם כאן ו-₪300 שם,
וביטול ששמר את הכסף אצלנו בלי רישום.

**לפני כל שינוי בתמחור, בקטלוג או בתקנון — לבדוק את הצד השני.**

- `app/data/{pricing,items,smallMoveItems}.js` **מקומפלים** מ-
  `~/Hovalot/src/data/*.ts`. לא לערוך אותם ביד.
- `app/legal.js`'s `TERMS_VERSION` חייב להסכים עם
  `~/Hovalot/src/constants/legal.ts`. הפורמט הוא `YYYY-MM-N` (ה-`N` נוסף
  ב-15.9 כי התקנון עודכן שלוש פעמים באותו חודש ו-`YYYY-MM` לבדו תייג
  נוסחים שונים כאותה הסכמה). `check-terms-version-sync.ts` בריפו השני אוכף.

⚠️ **עמוד הבית מציג מחיר אמיתי, לא הדגמה.** `index.html` מייבא
`BASE_PRICE`/`getVolumeDiscount` מ-`app/data/pricing.js`. שינוי בקובץ
התמחור משנה את המספר שרואים בעמוד הבית, לא רק בזרימת ההזמנה.

## שלוש בדיקות הקבלה — להריץ לפני קומיט
```
node scripts/check-a11y.mjs                       # 12 בדיקות, דפי HTML
node scripts/make-accessible-formats.mjs --check  # לא מפיק, לא דורש רשת
node scripts/check-app-web-const-sync.mjs         # קבועים מול Hovalot
```

**הראשונה** נוספה ב-15.9 אחרי שהתגלה שהצהרת הנגישות הבטיחה WCAG AA בלי
שהייתה בריפו ולו בדיקה אחת שתאכוף את זה. ההצהרה היא התחייבות משפטית —
בדיקה נופלת כאן היא חשיפה, לא אי-נוחות.

⚠️ **סדר ההרצה חשוב, וטעות בו עולה חצי שעה.** `stamp-assets` מזריק
`?v=<hash>` לכל הפניית נכס, ו**הוספת הפניה חדשה** (קובץ JS שלא היה)
מייתמת את הקבצים הנגישים — זו התנהגות מכוונת ומתועדת בסקריפט. לכן:
```
node scripts/stamp-assets.mjs            # קודם
node scripts/make-accessible-formats.mjs # ואז — 33 דקות הקראה
```
ב-18.9 הרצתי הפוך ושילמתי פעמיים.

**השנייה** שומרת על `accessible/` — קובצי הקול והטקסט הנגישים מופקים ע"י
`scripts/make-accessible-formats.mjs` (קול `Carmit` של macOS `say`),
ו-`accessible/manifest.json` מחזיק `sourceSha256` של מסמכי המקור. **כל
עריכה ב-`terms.html` או ב-`privacy.html` מייתמת אותם**, וצריך להפיק מחדש.
⚠️ `~/Hovalot/scripts/check-accessible-artifacts-fresh.ts` קורא לבדיקה
הזו — כלומר **עריכה כאן מפילה בדיקה בריפו האפליקציה**.

**השלישית** נוספה ב-17.9 אחרי שהתגלה שהאתר הרשה 10 תמונות/כותרת
100/תיאור 2000 במרקטפלייס בזמן שהאפליקציה מגבילה ל-4/40/600 — האתר יושר
מול תקרת `firestore.rules` (הגנת גודל מסמך) במקום מול הקבוע האפליקטיבי
שהיה אמור להיות מקור האמת לממשק. היא טבלת מיפוי מפורשת (לא רגקס גנרי
שמנחש זוגות) בין קבועי ולידציה/מגבלה/צינון-זמן ב-`~/Hovalot/src` לבין
המקבילה שלהם ב-`app/`, ונופלת גם אם דפוס החיפוש של שורה מסוימת מפסיק
להתאים לקוד (לא רק אם הערכים מתפצלים). דורשת `~/Hovalot` על הדיסק
(או `HOVALOT_ROOT`); אם הוא חסר היא מדלגת עם אזהרה במקום ליפול.
**לא בודקת תמחור/עמלות/ביטולים** — אלה כבר מכוסים בבדיקות הסנכרון
של ריפו האפליקציה (`cancellation-policy-prompt-sync` וכו', ב-Hovalot).

## מה שקל לפספס
- **דפי `preview-*.html` נגישים פומבית** ב-`rozicmove.com/preview-*.html`,
  למרות `noindex` ולמרות שאינם ב-`sitemap.xml` ואינם מקושרים משום מקום.
  הם משמשים להצגת כיווני עיצוב לגיא, **ויורדים אחרי שהוא מכריע.**
  ⚠️ 18.9 — הסעיף הזה נקב בשמות `preview-home.html` ו-`preview-heroes.html`
  שנמחקו מהריפו כבר ב-16.9. **לא לנקוב בשמות כאן** — הם משתנים בכל סבב
  עיצוב, והרשימה מתיישנת בשקט. `ls preview-*.html` נותן את האמת.
- ⚠️ **עמוד תצוגה נבנה ב-`cp index.html`, לא מאפס.** ב-17.9 נבנו ארבעה
  כיווני עיצוב כעמודים עצמאיים — הם לא טענו את `style.css`, לא כללו את
  ההירו, **וגיא פסל את כולם.** הבדיקה הזולה: `grep 'rel="stylesheet"'`
  ו-`grep 'class="hp-stage"'` על התוצאה.
- `robots.txt` מכיל הסבר למה `Disallow` **אינו** הדרך להוריד דף
  מהאינדקס — לקרוא אותו לפני שמוסיפים שם שורה.
- `app/pilot-banner.js` + `app/pilot-mode.js` החליפו את
  `under-construction-banner.js` הישן: כרטיס קופץ במסכי הבית שניתן
  לסגירה, במקום פס קבוע בראש כל דף.

## הרקע המלא
ההיסטוריה של האתר — 11 סבבי הבנייה של יולי, הפידבק של גיא על העיצוב,
פרשת מפתח ה-Gemini, ויום ה-15.9 במלואו — בזיכרון:
`~/.claude/projects/-Users-guyrozic/memory/project_rozic_move_web_ordering.md`.
