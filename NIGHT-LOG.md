# NIGHT-LOG — פאזה 0, ריצת לילה 16.9.2026

סוכן worktree מבודד, ענף `worktree-agent-ae98e621f95be33da`. לא נדחף ל-main,
לא נפרס לחי. שלושה ממצאים שדורשים תשומת לב — לא עצרו את הבנייה, ראו "מה
נבנה בכל זאת" בכל סעיף.

---

## 1. ⚠️ חסם פרטיות — צריך אישור לפני שההשלמה האוטומטית עולה לאוויר

**המשימה 0.2 ביקשה במפורש לרשום את זה, לפני שהתחלתי לממש.**

מימשתי השלמה אוטומטית (Google Places Autocomplete) בשדות הכתובת. המשמעות:
כל כמה תווים שמשתמש **מחובר** מקליד בשדה "כתובת מוצא"/"כתובת יעד" נשלחים
ל-Google (דרך פרוקסי `mapsProxy` של האפליקציה) — לפני שהוא סיים להקליד
ולפני שהוא לחץ על כפתור כלשהו.

בדקתי את `privacy.html` (שורה 209-211):

> "**Google Maps ו-Google Geocoding**: הכתובות שאתם מקלידים (מוצא ויעד)
> נשלחות ל-Google לצורך איתור המיקום, חישוב מרחק והצגת מפה. באתר
> rozicmove.com הכתובות עוברות דרך שרת שלנו אל Google."

זה **קרוב** לתאר את מה שקורה, אבל לא זהה: הנוסח הקיים נכתב (וסביר שתאם
בפועל, עד היום) למצב שבו רק **הכתובת המלאה שנשלחת בסוף** — בלחיצה על
"המשך"/גיאוקוד ברקע — עוברת לגוגל. השלמה אוטומטית שולחת **כל מצב ביניים
של ההקלדה עצמה** (כל תו-תו, כ-Autocomplete queries נפרדים), לא רק את
הכתובת הסופית. יכול להיות שהנוסח הקיים רחב מספיק לכסות גם את זה ("הכתובות
שאתם מקלידים" — ולא "הכתובת שאתם שולחים") — אבל זו קריאה, לא ודאות.

**מה שדורש את גיא:** לאשר שהנוסח הקיים ב-privacy.html מכסה גם שידור
תוך-כדי-הקלדה (לא רק הכתובת הסופית), או לעדכן את הנוסח לפני שגרסה עם
השלמה אוטומטית עולה ל-main/לייצור.

**מה נבנה בכל זאת (לא נתקעתי על זה):** ההשלמה האוטומטית ממומשת ועובדת
(ראו סעיף 2 לגבי מי בפועל מקבל אותה). `app/address-autocomplete.js` נושא
הערת ⚠️ בראש הקובץ שמצביעה לכאן.

---

## 2. ⚠️ חסם ארכיטקטורה — אין proxy נגיש לאורח, רק למשתמש מחובר

`geo.js:5` (לפני השינוי שלי) תיעד שהשלמה אוטומטית הושמטה במכוון. חיפשתי
לפי ההנחיה במשימה ("בדוק אם יש proxy כמו mapsProxy/geocodeAddress") ומצאתי
**שני** פרוקסי נפרדים ב-`Hovalot/functions/src/`:

| פרוקסי | דורש Firebase Auth token? | מכסה autocomplete/placeDetails? |
|---|---|---|
| `geocodeAddress.ts` | לא — Origin-allowlist בלבד (rozicmove.com) | לא, רק geocode חד-פעמי |
| `mapsProxy.ts` | **כן** — `verifyIdToken` חובה | כן — geocode+autocomplete+placeDetails |

`mapsProxy` נבנה במקור בשביל האפליקציה, ששם המשתמש **תמיד** מחובר. באתר
יש גם זרימת **אורח** (ראו `guest-checkout.js` — הזמנה שלמה בלי חשבון, עד
לפני התשלום). לאורח אין טוקן Firebase כלל (האתר לא עושה anonymous sign-in),
ולכן קריאה ל-`mapsProxy` מהאתר עבור אורח **תמיד** תיכשל ב-401.

**אין פרוקסי שלישי, ולא פרסתי אחד חדש.** פריסת Cloud Function חדשה היא
שינוי בתשתית ייצור **משותפת** בין האתר לאפליקציה (`hovalot-6cf65`), בריפו
אחר (`Hovalot`, לא `rozic-move`) — מחוץ להיקף worktree האתר, ודורשת מפתח
API/secret חדש, allowlist, ואישור פריסה. לא שלי לקחת כאן.

**ההחלטה שקיבלתי:**
- **משתמש מחובר** (יש לו `authState.authUser`, אותו טוקן ID תקף) — מקבל
  השלמה אוטומטית אמיתית דרך `mapsProxy`, בדיוק כמו באפליקציה (אותו קוד
  לוגי כמו `src/services/mapsApi.ts`, מפורש-מחדש כ-JS וניל ב-
  `app/address-autocomplete.js`).
- **אורח** (אין authState) — מקבל שדה חופשי + אימות רקע לא-חוסם דרך
  `geocodeAddress` הקיים (בדיוק מה שהיה גם קודם, רק עם badge "כתובת
  אומתה"/"לא הצלחנו לאמת"), ועם רמז עדין מתחת לשדה: "התחברות מאפשרת הצעות
  כתובת בזמן ההקלדה". אין דרך לתת לו dropdown הצעות בלי proxy נגיש.

**אומת בפועל (Playwright, ראו למטה):** מסלול האורח נבדק קצה-לקצה — כשל
ה-CORS/timeout מטופל בחן (badge אזהרה, לא שגיאה בקונסול). מסלול המשתמש
המחובר **לא** נבדק מול Firebase אמיתי (זה דורש התחברות לחשבון אמיתי מול
הפרויקט החי) — הקוד מראה נאמנות מדויקת ל-`mapsApi.ts`/`AddressInput.tsx`
המקוריים (אותו endpoint, אותם פרמטרים, אותו sessiontoken), אבל שווה בדיקת
עשן ידנית עם משתמש מחובר אמיתי לפני שסומכים על זה מלא.

**המלצה לגיא (לא חוסמת):** אם ההשלמה לאורח חשובה מספיק, האפשרויות הן (א)
anonymous sign-in לאורחים באתר (משנה את מודל ה-auth), או (ב) פרוקסי חדש
ב-Origin-allowlist כמו `geocodeAddress` אבל לשלושת ה-endpoints. שתיהן
מחוץ להיקף הריצה הזו.

---

## 3. ℹ️ ה-worktree לא נוצר מקצה הענף `website-build` בפועל

לא חסם — כבר תוקן. ל-registry: `website-build` (ה-HEAD שלו) הוא קומיט
`455084a` ("baseline לענף בניית האתר: חותמות בנייה + גרסת נכסים + תיקוני
צ'אט apartment (0.1)"), אבל ה-worktree הזה פוצל מ-`acd921c` — **ההורה
הישיר** של `455084a`, לא ממנו עצמו. כתוצאה מכך שני התיקונים ש"כבר בוצעו"
לפי תיאור המשימה (מיקום `#ai-questions`, שמירת שאלה+תשובה ל-`chatHistory`)
**לא** היו בפועל בעותק שלי של `apartment.html` — בדקתי את הקוד ישירות
ולא מצאתי אותם.

**מה שנעשה:** קראתי את הדיף המדויק מקומיט `455084a` (`git show 455084a --
app/apartment.html`) והחלתי אותו ידנית על `apartment.html` שלי (לא
cherry-pick לכל הקומיט — הוא נגע גם בקבצים שאינם שלי, כמו `login.html`
ו-`privacy.html`, עם חותמות-גרסה בלבד). התוצאה זהה למה שתיאור המשימה
הניח שכבר קיים. לא ידוע לי אם שאר סוכני הלילה המקבילים (יש שני worktree-
ים נוספים: `worktree-agent-a96d60b07d29ad4f9`,
`worktree-agent-addb11a5beb9a3476`) נפגעו מאותה בעיית פיצול — כדאי לבדוק
אצלם גם.

---

## מה נבנה — סיכום

ראו את הדיווח המלא בסוף השיחה. שני קומיטים על הענף:
1. `955b13d` — פאזה 0.1 (תיקוני צ'אט + "הפריטים שנבחרו")
2. (הקומיט של 0.2 — כתובות — יבוא אחרי כתיבת היומן הזה)
# NIGHT-LOG — פאזה 1 אחרי התשלום (16.9 לילה)

worktree: `agent-a96d60b07d29ad4f9` (בסיס `website-build`). לא נדחף ל-main, לא נפרס.

## מה נבנה

1. **"מחפשים מוביל" → "נמצא מוביל"** — משולב בתוך `app/order-status.html`
   עצמו (זה הדף שהלקוח נוחת עליו מיד אחרי `payment-success.html`, לא מסך
   נפרד): `status==='pending'` מציג כרטיס חיפוש עם נקודה פועמת ("מחפשים
   מוביל זמין..."); ברגע שהסטטוס יוצא מ-`pending` מופיע כרטיס מוביל
   ("נמצא לך מוביל!" ב-`assigned`, "המוביל שלך" אחרי מכן) עם שם, רכב,
   לוחית רישוי, תג "מאומת" וכפתור התקשרות.
2. **עומק order-status** — פס התקדמות 5 שלבים (מראה `OrderProgressBar.tsx`,
   פרספקטיבת לקוח בלבד), אישור סיום דו-צדדי (מוביל דיווח → הלקוח מאשר →
   `customerConfirmCompletion`), קישור חשבונית (`growInvoiceUrl`/
   `growInvoiceNumber`, שהיה קיים בנתונים אך לא מוצג באתר כלל), ביטול —
   **`en_route` נפתח לביטול** (חסר עד היום; מסך המקור מתיר את זה בחיוב
   75% קבוע). ⚠️ **לא נוסף חישוב תג-מחיר לפני האישור** — הוחלט במכוון
   לשמר את ההכרעה הקיימת מ-15.9 בקוד (`order-status.html`, ליד `cancel-btn`):
   השרת (`customerCancelOrder`) הוא מקור האמת היחיד לסכום, וחישוב שני
   בצד הלקוח הוא בדיוק סוג הפער שהוביל לבאג באתר בעבר. הודעת האזהרה
   הכללית עודכנה בניסוח בלבד כדי לכסות גם את `en_route`.
3. **מעקב חי** — `app/order-track.html` חדש. Leaflet + OpenStreetMap
   (בלי מפתח API — לא Google Maps, כי אין באתר כבר מפתח Maps JS חשוף
   בצד לקוח; גם ה-geocoding הקיים עובר דרך פרוקסי בשרת מאותה סיבה).
   סמני A/B מ-`fromLat/fromLng/toLat/toLng`, סמן מוביל חי מ-
   `orders/{id}.driverLocation`, חיווי טריות (חי/לא מתעדכן/מושהה) מראה
   `TrackOrderScreen`. **שני באגים אמיתיים נמצאו ותוקנו רק כי נבדק
   ב-Playwright, לא רק נקרא בעין:**
   - `direction: ltr` נדרש על מכל המפה — Leaflet ממקם הכול ב-
     `transform: translate3d(Xpx,Ypx,0)` שמניח פיקסלים משמאל-לימין;
     בירושת `dir="rtl"` מה-`<html>` בלי איפוס, חלק מהחישובים יצאו שגויים.
   - `animate:false` על `fitBounds`/`setView`/`panTo` — קריאה שנייה
     (panTo למיקום מוביל) תוך כדי אנימציית fitBounds עוד פעילה הזיזה
     סמן מאות פיקסלים מחוץ לתצוגה. נמדד ישירות (getBoundingClientRect
     על `.leaflet-marker-icon`), לא נוחש.
4. **צ'אט עם המוביל** — `app/order-chat.html` + `app/chat.js` חדשים.
   subscribe/send/markRead על `orders/{id}/messages`, אותה סכימה בדיוק
   שהאפליקציה קוראת וכותבת (`senderId/senderName/text/timestamp`).
5. **דירוג** — `app/order-rate.html` חדש. כוכבים חובה + תגיות (DRIVER_TAGS
   בלבד — האתר צד-לקוח) + טקסט חופשי + דירוג אפליקציה (כוכבים+טקסט),
   אחרי `completed`. כותב `driverRating`/`customerFeedback` בלבד
   (`submitCustomerRating` ב-`orders.js`), תואם ל-`customerRatesDriverOnly`
   ב-firestore.rules.

## קבצים חדשים
`app/users.js` (קריאת פרופיל מוביל בודד + formatVehicle), `app/chat.js`,
`app/order-track.html`, `app/order-chat.html`, `app/order-rate.html`.

## קבצים ששונו
`app/order-status.html` (העמקה מלאה — לא נגעתי בלוגיקת תשלום/pending_pricing
הקיימת, רק הוספתי סביבה), `app/orders.js` (תוספות בלבד: `ORDER_PROGRESS_STEPS`,
`customerConfirmCompletion`, `submitCustomerRating`, `subscribeToDriverLocation`
+ עזרי טריות, `getOrder` חד-פעמי — שום פונקציה קיימת לא נערכה), `app/style-app.css`
(תוספת בלבד, בסוף הקובץ).

## אומת
- `node scripts/check-a11y.mjs` — נקי (24 דפים, 12 בדיקות) אחרי כל השינויים.
- `node --check` על כל קובצי ה-JS (עצמאיים + מודולי ה-`<script type="module">`
  שחולצו מכל אחד מ-4 דפי ה-HTML).
- **צילומי מסך ב-Playwright headless** (1440 ומובייל 390) על כל 8 מצבי
  ההזמנה הרלוונטיים (`pending`, `assigned`, `en_route`+מעקב+צ'אט,
  `in_progress`+אישור-סיום-דו-צדדי, `completed` לא-מדורג, `pending_payment`,
  `pending_pricing`, `cancelled`) + שני דפי הצ'אט/דירוג.
  ⚠️ **בלי לגעת בפרויקט Firebase האמיתי**: הדפים עצמם (`order-status.html`
  וכו') נטענו כקבצים אמיתיים מהדיסק, אבל ה-imports ל-`auth.js`/`orders.js`/
  `users.js`/`chat.js`/`payments.js` יורטו ב-`page.route()` והוחלפו במודולים
  מדומים עם נתוני הזמנה מומצאים — שום כתיבה או קריאה אמיתית לא בוצעה מול
  `hovalot-6cf65`. זו הסיבה ששני הבאגים ב-#3 בכלל נתפסו.
- שני הבאגים שתוקנו (RTL/Leaflet) לא היו נראים מקריאת קוד בלבד — נתפסו
  רק כי צולם בפועל ונמדדו קואורדינטות הסמן במסך.

## מה לא מומש / הוחלט במכוון לדחות
- **בלי push לצ'אט**: `sendChatMessage` באתר לא שולח התראת Expo למוביל
  (בניגוד ל-`sendPushToUser` באפליקציה) — אין באתר גשר להתראות. המוביל
  יראה הודעה חדשה רק כשיפתח את הצ'אט שלו. מתועד בקוד (`app/chat.js`).
- **בלי "טען עוד" בצ'אט**: מוצגות 50 ההודעות האחרונות בלבד (כמו החלון
  ההתחלתי באפליקציה), בלי פאג'ינציה להיסטוריה ישנה יותר.
- **בלי דירוג ממוצע של המוביל** על כרטיס המוביל (`getDriverAverageRating`
  באפליקציה) — דורש שאילתה נוספת על היסטוריית הזמנות; לא חלק מ-5
  המשימות המקוריות, לא ממומש.
- **בלי דיווח "המוביל לא הגיע"/"הלקוח לא נמצא"**, בלי טיפ, בלי תמונות
  לפני/אחרי, בלי מועדף — אף אחד מהם לא היה בין 5 המשימות, לא נגעתי בהם
  כדי לא לחרוג מהיקף.
- דמי הביטול: כפתור הביטול נפתח גם ל-`en_route` (סעיף 2 למעלה), אבל שום
  מספר לא מחושב בצד הלקוח לפני האישור — החלטה מפורשת, לא פספוס. אם רוצים
  בעתיד טבלת מדרג לפני האישור (כמו באפליקציה) — זה שינוי ארכיטקטורה
  (להראות חישוב שני שיכול להיפרד מהשרת), לא באג.

## החלטות תכנון קטנות שכדאי לדעת
- כרטיס המוביל תמיד קורא את `users/{driverId}` ישירות מהדפדפן —
  `firestore.rules` מתיר `get` (לא `list`) לכל משתמש מחובר על כל מסמך
  משתמש בודד, בדיוק כמו שהאפליקציה מסתמכת עליו כדי להראות ללקוח את שם
  המוביל.
- `app/order-rate.html` משתמש ב-`getOrder` (קריאה חד-פעמית, לא מנוי חי)
  ולא ב-`subscribeToOrder` — כדי שסימון תגיות/כתיבת טקסט באמצע לא יימחקו
  אם המסמך יתעדכן משום סיבה אחרת בזמן שהטופס פתוח.

<<<<<<< HEAD
---

# NIGHT-LOG — פאזה 3: שערים ותמיכה (16.9.2026 לילה)

worktree: `agent-a6fb675b0c2a4d2fa`. **⚠️ ה-worktree נוצר על בסיס ישן —
לפני מיזוג פאזות 0-2 ל-`website-build`** (אותה תופעה שתועדה למעלה בסעיף 3
של פאזה 0, אצל worktree אחר). תוקן ב-fast-forward merge (`git merge
--ff-only website-build`) לפני שהתחלתי — היה נקי (0 קומיטים משלי על
הענף), כך שה-merge לא היה הרסני. בלעדיו הייתי בונה את שערי 3.1/3.2 על
`apartment.html`/`order-status.html` **בלי** `edit-profile.html`,
`auth.js`'s `isValidFullName`/`updateProfileFields` וכל שאר פאזה 2 —
התוצאה הייתה נשברת בשקט. worktree-ים מקבילים אחרים כדאי לבדוק אותו דבר.

## מה נבנה

**3.1 — שער טלפון מאומת לפני תשלום.** כבר היה קיים ב-`createOrder`
(apartment.html/small-move.html, מ-15.9) — **נוסף** גם ב-`order-status.html`'s
`pay-btn` (שלא היה חסום כלל: הזמנה שהגיעה ל-`pending_payment` בלי לעבור
דרך `createOrder` — הזמנה ישנה מלפני 15.9, או שתומחרה ידנית מ-
`pending_pricing` — הייתה יכולה להיות משולמת בלי טלפון מאומת). זהה
ל-`proceedToPayment` ב-`SummaryScreen.tsx`: טלפון קודם, שם מלא אחר כך.

**3.2 — שער שם מלא.** נוסף בשלוש הנקודות: `apartment.html`,
`small-move.html` (שתיהן ב-`createOrder`, אחרי שער הטלפון) ו-
`order-status.html` (`pay-btn`, מאותה סיבה כמו 3.1). משתמש ב-
`isValidFullName`/`FULL_NAME_ERROR` הקיימים כבר ב-`auth.js` (פאזה 2,
`edit-profile.html`) — לא מומש מחדש. מפנה ל-`edit-profile.html` הקיים.

**3.3 — צ'אט תמיכה AI.** `app/support-chat.html` חדש: צ'אט חי עם "רוני"
(אותו שם סוכן כמו באפליקציה), נושאי FAQ פתיחה (FAQ_TOPICS_CUSTOMER
בלבד — האתר הוא צד-לקוח), שאלות הבהרה עם כפתורי בחירה, הסלמה לנציג
אנושי (מעדכנת `support_tickets` לסטטוס `waiting` — אותו מנגנון בדיוק
כמו האפליקציה, כך שאדמין רואה זאת בכלי הקיימים), **וגם** קישורי יצירת
קשר ישיר קבועים (וואטסאפ 050-881-1085, מייל support@rozicmove.com) —
זמינים תמיד, לא רק אחרי הסלמה, כבקשת המשימה. `app/support.js` הורחב
מכתיבת-טופס-חד-פעמית למנגנון פנייה מלא (`createOrGetTicket`,
`sendSupportMessage`, `subscribeToTicketMessages/Ticket`,
`escalateToHuman`, `resolveTicket`, `markUserRead`) — אותה סכימת
Firestore בדיוק כמו `Hovalot/src/services/support.ts`, כך שפנייה שנפתחת
באתר מוצגת נכון ב-`AdminTicketDetailScreen`. `app/ai-vision.js` הורחב
ב-`chatSupportAgent` + `SUPPORT_SYSTEM_PROMPT` — **מועתק מילה במילה**
מ-`aiVisionCore.ts` (כולל מדרג הביטולים, שאלות ORDER NUMBER LOOKUP וכו'),
עם הענפים ה-driver-only מוסרים בכוונה (האתר צד-לקוח בלבד). ה-proxy
הקיים (`geminiProxy`, אותו אחד ש-`ai-vision.js` כבר משתמש בו לסריקת
תמונות) שירת גם את זה בלי שינוי — לא נדרש endpoint חדש.

הטופס החד-פעמי הישן ב-`account.html` (`createSupportTicket`) **הוחלף**
בכפתור לצ'אט החדש — היה כותב לאותה קולקציה בדיוק בלי לתת ללקוח לראות
תשובה, וצ'אט חי הוא שדרוג ולא תוספת מקבילה.

## ⚠️ 3.1 היא החלטת מדיניות — צריך את אישור גיא

מימשתי את שני השערים (טלפון + שם) בכל שלוש הנקודות, אבל השער על טלפון
מאומת מוסיף חיכוך אמיתי לפני תשלום — בדיוק כמו באפליקציה. **⚠️ הבהרה
חשובה:** זו לא החלטה חדשה שממתינה לאישור מאפס — שער הטלפון כבר **חי
בייצור** ב-`apartment.html`/`small-move.html` מ-15.9 (קומיט קודם, לא
שלי). מה שהתווסף כאן הוא **סגירת הפער** ב-`order-status.html` שהיה
פתוח עד היום. עדיין רושם כאן לפי הנחיית המשימה, כי זו עדיין החלטת
מדיניות (חיכוך ללקוח לפני כסף) שכדאי שגיא יאשר במפורש שהוא רוצה שהיא
תיסגר בכל שלוש הנקודות ולא רק בשתיים.

## אומת

**Playwright headless (Hovalot/node_modules), 1440 + נייד (390×844):**

- **3.1+3.2 — 4 קבצים, 3 תרחישים כל אחד** (טלפון לא מאומת → חסום; טלפון
  מאומת + שם חד-מילי → חסום עם קישור ל-`edit-profile.html`; שניהם
  תקינים → עובר). `order-status.html` נבדק בשני הרזולוציות; `apartment.html`/
  `small-move.html` נבדקו בליווי אשף מלא (בחירת פריט → דילוג אריזה →
  כתובות ותאריך → תנאים → שליחה) ב-1440 — ההיגיון בוליאני וזהה
  ל-`order-status.html` שכבר אומת בשתי הרזולוציות, ולכן לא שוכפל שם.
  `auth.js`/`orders.js`/`users.js`/`payments.js` יורטו ב-`page.route()`
  עם מודולים מדומים — שום כתיבה אמיתית ל-Firestore.
- **3.3 — שתי שכבות.** (א) בדיקה ממוחשבת מלאה עם `support.js`/`ai-vision.js`
  מדומים (בלי Firestore/Gemini אמיתיים): נושאים, שליחת הודעה, תשובת AI,
  שאלת הבהרה עם 3 סוגי אפשרות (ערך אמיתי/ביטול/`isChat`), הסלמה, סגירה
  ופתיחת פנייה חדשה — ב-1440 ונייד, אפס שגיאות קונסולה. **תוך כדי הבדיקה
  הזו נתפס באג אמיתי** (ראו למטה) ותוקן. (ב) **קריאת AI אמיתית אחת**:
  נרשם חשבון-בדיקה טרי (`webtest.phase3-support-*@rozicmove.com`, אין
  הרשאת אדמין למחוק — נבדק בפועל שאין service-account key בשני הריפואים,
  ראו ההערה למטה) מול הפרויקט האמיתי, נלחץ נושא "כמה עולה הובלה?", והתקבלה
  תשובה אמיתית מ-Gemini דרך `geminiProxy`: *"המחיר מחושב אוטומטית לפי
  מספר גורמים... עזרתי? יש עוד משהו שאוכל לעזור בו?"* — תואם בדיוק לחוק 1
  ולכלל "CLOSING A RESOLVED EXCHANGE" בפרומפט. שום קריאת AI שנייה לא בוצעה.
- `node scripts/check-a11y.mjs` — נקי, 28 דפים (כולל `support-chat.html`
  החדש), 12 בדיקות.
- `node scripts/make-accessible-formats.mjs --check` — נקי.
- `node scripts/stamp-assets.mjs` — הורץ, כל החותמות/גרסאות עדכניות.
- `node --check` על כל מודולי ה-`<script type="module">` שחולצו
  (apartment/small-move/order-status/account/support-chat.html) + על
  `ai-vision.js`/`support.js` כקבצים עצמאיים.

## ⚠️ באג אמיתי שנתפס ותוקן תוך כדי הבדיקה הממוחשבת

`support-chat.html`'s `sendMessage`: אחרי קבלת תשובת AI עם שאלות הבהרה,
`pendingQuestions` עודכן **אחרי** ה-`await sendSupportMessage(...)` של
תשובת ה-AI, בלי קריאה מפורשת ל-`renderQuestions()` אחרי העדכון. ל-
Firestore יש כתיבה אופטימית מקומית — ה-`onSnapshot` שמצייר את תשובת ה-AI
לרוב יורה **לפני** שה-`await` שמעליו בקוד בכלל חוזר, כלומר לפני ש-
`pendingQuestions` מתעדכן. בלי קריאה מפורשת, שאלת ההבהרה לא הייתה מוצגת
עד אירוע Firestore נוסף (או אף פעם, אם לא היה עוד אחד). זה בדיוק המקרה
שמדגים "בדיקה ירוקה יכולה להיות שבורה" הפוך: כאן בדיקה שנכתבה כמו שצריך
תפסה תזמון-מרוץ אמיתי לפני שהוא הגיע לייצור. תוקן: `renderQuestions()`
מפורש בשתי הנקודות (אחרי הוספת שאלות, ואחרי ניקוין בהסלמה אוטומטית).

## מה לא נבדק / הוחלט במכוון לדחות

- **מסך משוב אחרי `requestRating`** — האפליקציה מציגה כרטיס משוב
  in-app כש-AI מזהה לקוח מרוצה. לאתר אין מסך מקביל (`AppFeedbackScreen`
  לא הועתק לאתר בשום פאזה קודמת) — הדגל מתקבל ופשוט לא גורר שום UI.
  לא בין 3 המשימות, לא נבנה.
- **בדיקת מסלול שאלת-הבהרה מקצה-לקצה מול Gemini אמיתי** — נבדקה רק
  מוקאפ (בדיקה ממוחשבת). מסלול ה-flow המדויק (למשל "פריט נפגע" עם 4
  השאלות שלו ברצף) לא נבדק מול Gemini אמיתי כדי לא לחרוג מ"קריאה
  בודדת" — הפרומפט עצמו מועתק מילה במילה ונבדק כבר מול Gemini אמיתי
  באפליקציה (`eval:support-ai`, 13 מקרים), כך שהסיכון האמיתי כאן הוא
  רק בשכבת ה-UI באתר, וזו נבדקה.
- **חשבון הבדיקה שנוצר בפרודקשן לא נמחק** — אין service-account/admin
  key בשום מקום בשני הריפואים (נבדק במפורש), ואותה שיטה בדיוק שימשה
  בפאזות 0-2 (רישום חשבון-זבל טרי דרך הטופס האמיתי). המייל מתויג
  בבירור (`webtest.phase3-support-<timestamp>@rozicmove.com`, שם
  התצוגה "בדיקת פאזה 3 (למחיקה)") למי שירצה לנקות.
=======
# NIGHT-LOG — פאזה 4, ליטוש חזותי (16.9 לילה)

worktree: `agent-adb0cb15a9bd32f4f` (בסיס `website-build`, מוזג בפועל
מ-`c497395` כדי לקבל את פאזות 1-2 שנוצרו אחרי שה-worktree פוצל — ראו סעיף
3 בלוג של פאזה 0 למעלה, אותה תקלת-פיצול בדיוק). לא נדחף ל-main, לא נפרס.

## הקבצים שלי
`app/order-track.html`, `app/order-chat.html`, `app/order-rate.html`,
`app/edit-profile.html`, `app/settings.html`, `app/saved-addresses.html`.
לא נגעתי ב-`app/style-app.css` (משותף) — התיקון היחיד שדרש CSS נכתב
inline באחד מהדפים שלי.

## מה אומת
קראתי את הרפרנס (`RatingScreen`, `ChatScreen`, `TrackOrderScreen`,
`EditProfileScreen`, `SavedAddressesScreen` ב-`Hovalot/src/screens/shared`)
והשוויתי מקרוב לצבעים/מרווחים/רדיוסים בפועל. מצאתי ש-`style-app.css`
כבר נבנה בקפידה יוצאת דופן בפאזות 0-2 (הערות מדויקות עם מדידות, לא רק
טענות) — כל שישה הדפים כבר תואמים את פלטת ה-pine/ירוק, הטיפוגרפיה,
הכרטיסים, ה-RTL והמרווחים כמעט לגמרי.

**צילומי מסך ב-Playwright headless (1440 + iPhone 13)**, מוקאפ ES-module
דרך `page.route()` (בדיוק השיטה מפאזה 1 — `auth.js`/`orders.js`/`users.js`/
`chat.js`/`account.js`/`firebase.js` מוחלפים במודולים מדומים; שום קריאה
אמיתית ל-Firebase). כיסיתי **23 מצבים**: כל דף במצב בסיס בשני viewport-ים,
ובנוסף — כוכבים/תגיות מסומנים בדירוג, "כבר דירגתם", צ'אט ריק, צ'אט בלי
מוביל משובץ, מעקב "ממתינים למיקום", מעקב "מיקום לא מתעדכן" (stale),
כתובות שמורות ריק, כתובות שמורות במכסה (8/8), הגדרות עם רשימת חסומים
ריקה, עריכת פרופיל שגיאת ולידציה, עריכת פרופיל הצלחה. `node scripts/
check-a11y.mjs` נשאר נקי (27 דפים, 12 בדיקות) אחרי כל שינוי. בלי קריאות AI.

⚠️ **ממצא שווא, לא באג**: בצילומי `fullPage:true` של דפים גבוהים מ-1000px
(viewport 1440×1000), התג "עודכן..." וקישור "דלג לתוכן המרכזי" מופיעים
כפולים באמצע התמונה. שניהם `position:fixed` ב-`style.css`
(`.build-stamp`/`.skip-link`) — זו תקלת stitching ידועה של Chromium
headless בצילום full-page (כל "פרוסה" גוללת מציירת מחדש את האלמנטים ה-
fixed שלה), לא משהו שמשתמש אמיתי רואה בגלילה רגילה. אומת מול `style.css`
(שתי הכללים `position:fixed`) ולא תוקן.

## שני באגים אמיתיים נמצאו ותוקנו (נמדדו, לא נוחשו)

1. **`app/saved-addresses.html` — כפתור "הוסף" לא מיושר לשדה הכתובת.**
   `getBoundingClientRect()` לפני התיקון: קלט `top:681`, כפתור `top:668`
   — 13px גבוה מדי. הסיבה: `style="margin-top:22px"` על הכפתור הניח
   שהתווית מעליו (`label`) גובהה 22px בדיוק; בפועל 29px (רוחב הטקסט
   בפועל וה-line-height של Heebo ב-1440 גורמים לגלישה/מרווח שונה מהמשוער).
   **התיקון**: הסרתי את הפריצה הידנית והוספתי כלל CSS ייעודי לעמוד (לא
   ל-`style-app.css` המשותף) — `#add-form-wrap .inline-form-row {
   align-items: flex-end }` תחת `@media (min-width:481px)`, כדי לא להתנגש
   עם כלל הנייד הקיים (`max-width:480px`, שכבר הופך את השורה לעמודה
   `align-items:stretch`). אחרי התיקון: קלט וכפתור `top:681,bottom:736`
   **זהים**, כי לשניהם יש בפועל אותו גובה (55px) — `align-items:flex-end`
   מיישר אותם בלי תלות בגובה התווית.

2. **`app/saved-addresses.html` — הודעת מכסת הכתובות (8/8) לא נראית
   בכלל.** ה-JS כותב את הטקסט ל-`#addresses-limit-hint`, אבל האלמנט
   הזה ישב **בתוך** `#add-form-wrap` — והקוד מסתיר את כל ה-wrap הזה
   ברגע שמגיעים למכסה (`display:none`). כלומר המשתמש שמילא 8 כתובות
   רואה את טופס ההוספה נעלם, ובלי שום הסבר למה — לא שגיאה, לא הודעה,
   כלום. **התיקון**: הוצאתי את `<p id="addresses-limit-hint">` מחוץ
   ל-`#add-form-wrap`, כך שהיא ממשיכה להיות מוצגת גם כש-wrap מוסתר.
   אומת בצילום מסך: "הגעת ל-8 הכתובות השמורות. כדי להוסיף כתובת חדשה
   יש למחוק אחת מהרשימה קודם." מופיע עכשיו מתחת לרשימה.

## מה לא שונה, ולמה
- `SavedAddressesScreen.tsx` באפליקציה משתמש בדפוס "כפתור מקווקו שפותח
  טופס נפרד עם Save/Cancel", לא בשורת קלט+כפתור קבועה כמו באתר. זה שוני
  ארכיטקטוני שכבר הוחלט בפאזה 2, לא חוסר-עקביות חזותית — לא שיניתי אותו
  (מחוץ להיקף "ליטוש חזותי", ולא התבקשתי לבנות מחדש את הזרימה).
- מצבי-ריק הקומפקטיים בתוך כרטיס (`#addresses-empty`, `#blocked-empty`)
  מוצגים כטקסט מרוכז בלי אייקון, בשונה ממצבי-שגיאה במסך שלם (❓/🚫/💬
  ב-order-track/chat/rate). זה עקבי בין שני המקומות שבהם הוא מופיע
  (settings.html + saved-addresses.html) — לא תיקנתי, כי זו כבר התאמה
  מכוונת לגודל ה"ריק" (קטע בתוך עמוד גדול, לא מסך שגיאה מלא).

## קבצים ששונו
`app/saved-addresses.html` בלבד (שני התיקונים למעלה). שאר חמשת הדפים
שלי נבדקו לעומק ולא נמצאו בהם באגי יישור/עקביות/רספונסיביות שדרשו שינוי.
>>>>>>> worktree-agent-adb0cb15a9bd32f4f
