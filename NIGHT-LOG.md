# NIGHT-LOG — פאזה 1 אחרי התשלום (16.9 לילה)

worktree: `agent-a96d60b07d29ad4f9` (בסיס `website-build`). לא נדחף ל-main, לא נפרס.

## מה נבנה

1. **"מחפשים מוביל" → "נמצא מוביל"** — משולב בתוך `app/order-status.html`
   עצמו (זה הדף שהלקוח נוחת עליו מיד אחרי `payment-success.html`, לא מסך
   נפרד): `status==='pending'` מציג כרטיס חיפוש עם נקודה פועמת ("מחפשים
   מוביל זמין..."); ברגע שהסטטוס יוצא מ-`pending` מופיע כרטיס מוביל
   ("נמצא לך מוביל!" ב-`assigned`, "המוביל שלך" אחרי מכן) עם שם, רכב,
   לוחית רישוי, תג "מאומת" וכפתור התקשרות.
2. **עומק order-status** — פס התקדמות 5 שלבים (מראה `OrderProgressBar.tsx`,
   פרספקטיבת לקוח בלבד), אישור סיום דו-צדדי (מוביל דיווח → הלקוח מאשר →
   `customerConfirmCompletion`), קישור חשבונית (`growInvoiceUrl`/
   `growInvoiceNumber`, שהיה קיים בנתונים אך לא מוצג באתר כלל), ביטול —
   **`en_route` נפתח לביטול** (חסר עד היום; מסך המקור מתיר את זה בחיוב
   75% קבוע). ⚠️ **לא נוסף חישוב תג-מחיר לפני האישור** — הוחלט במכוון
   לשמר את ההכרעה הקיימת מ-15.9 בקוד (`order-status.html`, ליד `cancel-btn`):
   השרת (`customerCancelOrder`) הוא מקור האמת היחיד לסכום, וחישוב שני
   בצד הלקוח הוא בדיוק סוג הפער שהוביל לבאג באתר בעבר. הודעת האזהרה
   הכללית עודכנה בניסוח בלבד כדי לכסות גם את `en_route`.
3. **מעקב חי** — `app/order-track.html` חדש. Leaflet + OpenStreetMap
   (בלי מפתח API — לא Google Maps, כי אין באתר כבר מפתח Maps JS חשוף
   בצד לקוח; גם ה-geocoding הקיים עובר דרך פרוקסי בשרת מאותה סיבה).
   סמני A/B מ-`fromLat/fromLng/toLat/toLng`, סמן מוביל חי מ-
   `orders/{id}.driverLocation`, חיווי טריות (חי/לא מתעדכן/מושהה) מראה
   `TrackOrderScreen`. **שני באגים אמיתיים נמצאו ותוקנו רק כי נבדק
   ב-Playwright, לא רק נקרא בעין:**
   - `direction: ltr` נדרש על מכל המפה — Leaflet ממקם הכול ב-
     `transform: translate3d(Xpx,Ypx,0)` שמניח פיקסלים משמאל-לימין;
     בירושת `dir="rtl"` מה-`<html>` בלי איפוס, חלק מהחישובים יצאו שגויים.
   - `animate:false` על `fitBounds`/`setView`/`panTo` — קריאה שנייה
     (panTo למיקום מוביל) תוך כדי אנימציית fitBounds עוד פעילה הזיזה
     סמן מאות פיקסלים מחוץ לתצוגה. נמדד ישירות (getBoundingClientRect
     על `.leaflet-marker-icon`), לא נוחש.
4. **צ'אט עם המוביל** — `app/order-chat.html` + `app/chat.js` חדשים.
   subscribe/send/markRead על `orders/{id}/messages`, אותה סכימה בדיוק
   שהאפליקציה קוראת וכותבת (`senderId/senderName/text/timestamp`).
5. **דירוג** — `app/order-rate.html` חדש. כוכבים חובה + תגיות (DRIVER_TAGS
   בלבד — האתר צד-לקוח) + טקסט חופשי + דירוג אפליקציה (כוכבים+טקסט),
   אחרי `completed`. כותב `driverRating`/`customerFeedback` בלבד
   (`submitCustomerRating` ב-`orders.js`), תואם ל-`customerRatesDriverOnly`
   ב-firestore.rules.

## קבצים חדשים
`app/users.js` (קריאת פרופיל מוביל בודד + formatVehicle), `app/chat.js`,
`app/order-track.html`, `app/order-chat.html`, `app/order-rate.html`.

## קבצים ששונו
`app/order-status.html` (העמקה מלאה — לא נגעתי בלוגיקת תשלום/pending_pricing
הקיימת, רק הוספתי סביבה), `app/orders.js` (תוספות בלבד: `ORDER_PROGRESS_STEPS`,
`customerConfirmCompletion`, `submitCustomerRating`, `subscribeToDriverLocation`
+ עזרי טריות, `getOrder` חד-פעמי — שום פונקציה קיימת לא נערכה), `app/style-app.css`
(תוספת בלבד, בסוף הקובץ).

## אומת
- `node scripts/check-a11y.mjs` — נקי (24 דפים, 12 בדיקות) אחרי כל השינויים.
- `node --check` על כל קובצי ה-JS (עצמאיים + מודולי ה-`<script type="module">`
  שחולצו מכל אחד מ-4 דפי ה-HTML).
- **צילומי מסך ב-Playwright headless** (1440 ומובייל 390) על כל 8 מצבי
  ההזמנה הרלוונטיים (`pending`, `assigned`, `en_route`+מעקב+צ'אט,
  `in_progress`+אישור-סיום-דו-צדדי, `completed` לא-מדורג, `pending_payment`,
  `pending_pricing`, `cancelled`) + שני דפי הצ'אט/דירוג.
  ⚠️ **בלי לגעת בפרויקט Firebase האמיתי**: הדפים עצמם (`order-status.html`
  וכו') נטענו כקבצים אמיתיים מהדיסק, אבל ה-imports ל-`auth.js`/`orders.js`/
  `users.js`/`chat.js`/`payments.js` יורטו ב-`page.route()` והוחלפו במודולים
  מדומים עם נתוני הזמנה מומצאים — שום כתיבה או קריאה אמיתית לא בוצעה מול
  `hovalot-6cf65`. זו הסיבה ששני הבאגים ב-#3 בכלל נתפסו.
- שני הבאגים שתוקנו (RTL/Leaflet) לא היו נראים מקריאת קוד בלבד — נתפסו
  רק כי צולם בפועל ונמדדו קואורדינטות הסמן במסך.

## מה לא מומש / הוחלט במכוון לדחות
- **בלי push לצ'אט**: `sendChatMessage` באתר לא שולח התראת Expo למוביל
  (בניגוד ל-`sendPushToUser` באפליקציה) — אין באתר גשר להתראות. המוביל
  יראה הודעה חדשה רק כשיפתח את הצ'אט שלו. מתועד בקוד (`app/chat.js`).
- **בלי "טען עוד" בצ'אט**: מוצגות 50 ההודעות האחרונות בלבד (כמו החלון
  ההתחלתי באפליקציה), בלי פאג'ינציה להיסטוריה ישנה יותר.
- **בלי דירוג ממוצע של המוביל** על כרטיס המוביל (`getDriverAverageRating`
  באפליקציה) — דורש שאילתה נוספת על היסטוריית הזמנות; לא חלק מ-5
  המשימות המקוריות, לא ממומש.
- **בלי דיווח "המוביל לא הגיע"/"הלקוח לא נמצא"**, בלי טיפ, בלי תמונות
  לפני/אחרי, בלי מועדף — אף אחד מהם לא היה בין 5 המשימות, לא נגעתי בהם
  כדי לא לחרוג מהיקף.
- דמי הביטול: כפתור הביטול נפתח גם ל-`en_route` (סעיף 2 למעלה), אבל שום
  מספר לא מחושב בצד הלקוח לפני האישור — החלטה מפורשת, לא פספוס. אם רוצים
  בעתיד טבלת מדרג לפני האישור (כמו באפליקציה) — זה שינוי ארכיטקטורה
  (להראות חישוב שני שיכול להיפרד מהשרת), לא באג.

## החלטות תכנון קטנות שכדאי לדעת
- כרטיס המוביל תמיד קורא את `users/{driverId}` ישירות מהדפדפן —
  `firestore.rules` מתיר `get` (לא `list`) לכל משתמש מחובר על כל מסמך
  משתמש בודד, בדיוק כמו שהאפליקציה מסתמכת עליו כדי להראות ללקוח את שם
  המוביל.
- `app/order-rate.html` משתמש ב-`getOrder` (קריאה חד-פעמית, לא מנוי חי)
  ולא ב-`subscribeToOrder` — כדי שסימון תגיות/כתיבת טקסט באמצע לא יימחקו
  אם המסמך יתעדכן משום סיבה אחרת בזמן שהטופס פתוח.
