# דוח סנכרון לוגיקה עסקית — אתר מול אפליקציה

**תאריך:** 16.9.2026
**שיטה:** קריאה ישירה של כל קבצי המקור בשני הריפואים + הרצת שתי בדיקות
הסנכרון האוטומטיות הקיימות בריפו האפליקציה (`check-web-pricing-sync.ts`,
`check-web-catalog-sync.ts`, `check-terms-version-sync.ts`) לאימות סופי.
כל שלוש רצו בפועל והחזירו ✓. זהו audit בלבד — לא נערך שום קובץ.

## תוצאה מסכמת
**אין דריפט כספי חדש.** שני הפערים הכספיים שהיו ידועים מ-15.9 (ביטוח
בשלב הרצה, ומחיר מנוף בקומת קרקע) כבר תוקנו — האחרון תוקן היום, 16.9,
לפי חותמת ההערות ב-`app/geo.js`. נמצא פער אחד לא-כספי (טבלאות מנוף
מתות שנשארו בקובץ מקומפל), כבר מתועד ומעוקב בבדיקה האוטומטית.

---

## 1. תמחור — **מסונכרן במלואו**

השוואתי `rozic-move/app/data/pricing.js` מול `Hovalot/src/data/pricing.ts`
ערך-ערך. זהים לחלוטין: `BASE_PRICE` (150), `PRICE_PER_KM` (9),
`FLOOR_SURCHARGE_PER_FLOOR` (40), `PLATFORM_FEE_RATE` (0.10),
`INSURANCE_PRICE_APARTMENT` (99), `INSURANCE_PRICE_SMALL_MOVE` (49),
`PACKING_BASE_FEE` (149), `PACKING_PERCENT_RATE` (0.30), כל 20 המדרגות
של `VOLUME_DISCOUNT`, כל 5 המדרגות של `CANCELLATION_POLICY`, `BOX_PRICES`
(S/M/L/XL), וכל 262 הערכים ב-`ITEM_PRICES` על פני שמונה הקטגוריות.

הרצתי את `check-web-pricing-sync.ts` (משווה גם התנהגות בפועל של
`getSurgePricing`/`getVolumeDiscount`/`getCancellationFee` על תרחישי גבול,
לא רק את הטבלאות): **349 ערכים בטבלאות הגדולות + 28 קריאות תמחור נבדקו
— ✓ אין דריפט חדש.**

**[כספי: לא] `SMALL_MOVE_BASE_PRICE` — אתר: קשיח (`const BASE = 100`
בתוך `small-move.html`) · אפליקציה: קבוע מיוצא (`SMALL_MOVE_BASE_PRICE`
ב-`pricing.ts`, כרגע 100)** — הערך זהה (100=100) ואין טעות היום, אבל
`pricing.js` המקומפל **לא מייצא** את הקבוע הזה בכלל, ולכן `small-move.html`
עוקף את זה עם ספרה קשיחה בתוך ה-HTML במקום לייבא. שינוי עתידי ל-
`SMALL_MOVE_BASE_PRICE` באפליקציה לא יגיע לאתר אוטומטית — מישהו חייב
לזכור לערוך גם את השורה הקשיחה. בדיקת `check-web-pricing-sync.ts` כבר
מכסה את זה במפורש (`HARDCODED`), כך שדריפט עתידי כן ייתפס.

## 2. קטלוג — **מסונכרן במלואו**

השוואתי `app/data/items.js` מול `src/data/items.ts` (474/726 שורות,
כולל השוואת כל שדה של כל פריט — מפתח, תווית, אייקון, כמות ברירת מחדל,
heavy/fragile/needsDisassembly, הערות, נרדפים, שיוך לחדר) וכן
`app/data/smallMoveItems.js` מול `src/data/smallMoveItems.ts`.

הרצתי את `check-web-catalog-sync.ts`: **243 פריטים · 8 חדרים · 285
נרדפים · 243 סיווגי אריזה נבדקו — ✓ הקטלוגים זהים.** זה כולל גם השוואת
`getFullAICatalog()` בפועל (מה שבאמת מוזרק לפרומפט ה-AI בשני הצדדים) וגם
סיווג האריזה (`getPackingInfo`, `PACKING_NONE_KEYS`, `PACKING_WRAP_KEYS`)
— זיהיתי ואימתתי ידנית ששתי הרשימות (כ-90+90 מפתחות) זהות תו-בתו.

ההערה בהערת המשימה התבררה נכונה בפועל: **הקבצים המקומפלים (`items.js`,
`smallMoveItems.js`) אכן עדכניים** ומשקפים במדויק את מקור האמת. לעומת
זאת `pricing.js` **אינו** עדכני במלואו — ראו סעיף 5.

`smallMoveItems.ts` באפליקציה מכיל גם לוגיקת הגבלת-נפח לטנדר
(`unitVolumeOf`/`totalVolumeOf`/`applyWithinCapacity`) שאין לה מקבילה
באתר. זו לוגיקת UX (מניעת חריגה מ-`MAX_VOLUME`), לא תמחור — אינה
משפיעה על שום מספר שהלקוח משלם, ולכן לא סימנתי אותה כממצא כספי.

## 3. תקנון — **מסונכרן במלואו**

`TERMS_VERSION = '2026-09-3'` זהה מילה-במילה ב-`app/legal.js` וב-
`src/constants/legal.ts`. הרצתי את `check-terms-version-sync.ts`:
**✓ הקבוע בשני המשטחים וחמשת המסמכים מסכימים** (`terms.html`,
`privacy.html`, `accessibility.html`, `TermsOfServiceScreen.tsx`,
`PrivacyPolicyScreen.tsx` — כולם "ספטמבר 2026").

מנגנון רישום ההסכמה (`orderTermsConsent()` באתר מול אותה פונקציה
באפליקציה) שולח את אותם ארבעה מפתחות בדיוק; ההבדל היחיד — `termsAcceptedVia`
('order_checkbox' באתר מול 'registration' באפליקציה) — מתועד במפורש
כהבדל מכוון (רגע ההסכמה שונה בין שני הזרמים), לא דריפט.

## 4. מדרג ביטולים — **מסונכרן במלואו**

`CANCELLATION_POLICY` זהה ב-`pricing.js`/`pricing.ts` (7+ ימים חינם ·
2–7 ימים 15% · 24–48 שעות 30% · פחות מ-24 שעות 50% · אי-הופעה 75%),
ואומת גם בהתנהגות בפועל של `getCancellationFee` על 12 ערכי גבול (0-,
0, 1, 23, 24, 25, 47, 48, 49, 167, 168, 169 שעות) בבדיקה האוטומטית —
תוצאה זהה בשני הצדדים בכל נקודה.

הנוסח הצרכני ב-`terms.html` סעיף 7 זהה מילה-במילה ל-`TermsOfServiceScreen.tsx`
סעיף 7, כולל מדרג הקנס לצד המוביל (מעל 24 שעות = ₪100, מתחת ל-24 שעות
= ₪150) והפיקדון (`DRIVER_DEPOSIT_AMOUNT = 150`).

האתר (`order-status.html`) בכוונה **אינו** מציג ללקוח סכום ביטול מחושב
מראש — ההערה בקוד מסבירה שזה תיקון מכוון מ-15.9 אחרי שמספר שחושב בצד
הלקוח הוצג שגוי; הסכום נגזר תמיד בשרת (`customerCancelOrder`). זו
פרקטיקה טובה יותר מהאפליקציה שמציגה תחזית, לא דריפט.

## 5. מנוף / ביטוח / אריזה

**ביטוח — מסונכרן.** `INSURANCE_PRICE_APARTMENT`=99 ו-
`INSURANCE_PRICE_SMALL_MOVE`=49 זהים. חשוב מכך: שני הצדדים מאפסים נכון
את דמי הביטוח בשלב ההרצה (`PILOT_MODE`) — אימתתי שגם `apartment.html`
וגם `small-move.html` מייבאים ומפעילים את `insurancePriceInPilot()`
מ-`app/pilot-mode.js`, שמקביל מדויק ל-`src/constants/pilotMode.ts`
באפליקציה. **זה בדיוק אותו סוג ממצא כספי כמו זה שהמשימה מזכירה כדוגמה
(ביטוח שנגבה בניגוד לתקנון)** — לפי ההערות בקוד הוא נמצא ותוקן באפליקציה
ב-15.9 ובאתר **היום, 16.9** (ראו `app/pilot-mode.js`). נכון לרגע כתיבת
הדוח: מסונכרן, ללא פער.

**אריזה — מסונכרן.** `PACKING_BASE_FEE`=149, `PACKING_PERCENT_RATE`=0.30
זהים; לוגיקת `calculatePackingFromClassified`/`classifyRoomItemForPacking`
זהה תו-בתו בין שני הצדדים.

**מנוף — מסונכרן במחיר בפועל, עם שריד לא-כספי.**
טבלת המנוף שבאמת מחוברת ל-UI היא `CRANE_PRICE_PER_FLOOR` — באתר יושבת
ב-`app/geo.js` (לא ב-`pricing.js`!), באפליקציה ב-`pricing.ts`. לכל
הקומות הניתנות לבחירה בפועל בשני הצדדים (קרקע/1/2/3/4/5/6+) הערכים
זהים: 300/300/400/500/600/700/900. **זו בדיוק הדוגמה שהמשימה נתנה
("מנוף שהיה חינם באתר ו-₪300 באפליקציה")** — לפי ההערה המפורטת ב-
`app/geo.js` זה היה המצב בפועל עד היום: קומת קרקע הייתה ₪0 באתר מול
₪300 באפליקציה, **ותוקן היום (16.9)** ליישור מלא לפי הכרעת גיא מ-11.9.
נכון לרגע כתיבת הדוח, אין פער.

**[כספי: לא] שש טבלאות מנוף ישנות עדיין קיימות ב-`pricing.js` — אתר:
`APARTMENT_CRANE_PRICE_PER_HOUR`, `INDUSTRIAL_CRANE_PRICES`, `CRANE_ITEMS`,
`CRANE_ITEM_CATEGORIES`, `CRANE_LOAD_TYPES`, `CRANE_WEIGHT_OPTIONS` ·
אפליקציה: נמחקו לגמרי מ-`pricing.ts`** ("כאן ישבו שש טבלאות מנוף... הוסרו
לפי החלטה", לפי ההערה במקור). אימתתי בגריפ מלא שאף קובץ באתר לא מייבא
אף אחת משש הטבלאות האלה — קוד מת ב-100%, בלי השפעה על שום מחיר שהלקוח
רואה (המנוף בפועל מתומחר רק דרך `CRANE_PRICE_PER_FLOOR` ב-`geo.js`).
**המשמעות:** זו בדיוק אזהרת "לוודא שהקבצים המקומפלים באמת עדכניים"
מתוך הנחיית המשימה — `pricing.js` **אינו** עדכני במלואו מול `pricing.ts`,
בניגוד ל-`items.js`/`smallMoveItems.js` שכן. הפער כבר מתועד ומעוקב
במפורש בקוד הבדיקה (`WEB_ONLY_TABLES` ב-`check-web-pricing-sync.ts`),
עם ההערה "ההכרעה פתוחה לגיא" — כלומר זו החלטת ניקיון קוד ממתינה, לא
ממצא חדש. לא נגעתי בקובץ (audit בלבד).

---

## מה שהרצתי בפועל (לשקיפות)
```
npx tsx scripts/check-web-pricing-sync.ts    → ✓ אין דריפט חדש (349+28 בדיקות)
npx tsx scripts/check-web-catalog-sync.ts    → ✓ הקטלוגים זהים (243 פריטים)
npx tsx scripts/check-terms-version-sync.ts  → ✓ מוסכם בכל חמשת המסמכים
```
