// Same key the mobile app already ships client-side as EXPO_PUBLIC_GEMINI_API_KEY
// (get one free at https://aistudio.google.com/apikey, or reuse the existing one —
// it's already exposed in the app bundle today, this isn't a new exposure).
// Fill this in, then redeploy the site — nothing AI-related works until you do.
export const GEMINI_API_KEY = '';
