/**
 * שלב ההרצה באתר — **תאום ל-`src/constants/pilotMode.ts` באפליקציה.**
 *
 * ## ⚠️ 16.9 — למה זה נוצר, והבאג שהוא סוגר
 * התקנון, בשני המשטחים, מבטיח במילים האלה:
 * *"כל עוד האפליקציה בשלב ההרצה… **הכיסוי אינו פעיל ולא ייגבו בגינו
 * דמי כיסוי**."*
 *
 * ב-15.9 הבאג הזה תוקן ב**אפליקציה** — `insurancePriceInPilot()` מחזירה
 * 0 והמתג נשאר, כך שהלקוח מתנסה בכל המסלול בלי לשלם על כיסוי שאינו
 * פעיל. ההערה שם אפילו כותבת "וכך גם האתר".
 *
 * ⚠️ **והאתר המשיך לגבות ₪99 / ₪49.** אותו תיקון בדיוק, בוצע בצד אחד.
 * זה המשך ישיר של הלקח שנכתב שם: *נוסח שמבטיח התנהגות חייב להיכתב
 * יחד עם הקוד שמקיים אותה* — ומסתבר שגם **בכל המשטחים שהנוסח חל
 * עליהם**, לא רק בזה שבו הבחינו.
 *
 * ⚠️ **לכיבוי ביום שהפעילות מתחילה:** `PILOT_MODE = false` **בשני
 * הקבצים באותו קומיט**, ובאותו יום לעדכן גם את שני התקנונים — הפסקאות
 * שם מנוסחות "כל עוד האפליקציה בשלב ההרצה" ונשארות שקריות אם הדגלים
 * לבדם ירדו.
 */
export const PILOT_MODE = true;

/** דמי ביטוח התכולה בשלב ההרצה — **אפס**. ראו ההערה למעלה. */
export function insurancePriceInPilot(fullPrice) {
  return PILOT_MODE ? 0 : fullPrice;
}
